/*jshint -W117 */
/*jshint -W106 */
define(['angular',
    './../../modules/sample-module/sample-module'
], function (angular, controllers) {
    'use strict';

    // Controller definition
    controllers.controller('homeCtrl',
        ['$interval', '$scope', function ($interval, $scope) {
        // PUBLIC METHODS AND VARS (in $scope)
        // ======================
    }]);
});
